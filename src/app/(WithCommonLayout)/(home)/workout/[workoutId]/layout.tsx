import { ReactNode } from "react"

const WorkoutDetailsLayout = ({ children }: { children: ReactNode }) => {
    return (
        <div className="">
            {children}
        </div>
    )
}

export default WorkoutDetailsLayout 