import { ReactNode } from "react"

const NutritionDetailsLayout = ({ children }: { children: ReactNode }) => {
    return (
        <div className="">
            {children}
        </div>
    )
}

export default NutritionDetailsLayout 